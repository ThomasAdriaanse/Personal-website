version: '3.8'

services:
  flask-portfolio:
    image: thomasadriaanse/flask-portfolio-simple:latest
    ports:
      - "5000:5000"
    volumes:
      - .:/flask-portfolio

      
    # environment:
    #   - FLASK_ENV=development
    # command: ["pipenv", "run", "flask", "--debug", "--app", "api/index.py", "run", "--host=0.0.0.0", "--port=6001"]
